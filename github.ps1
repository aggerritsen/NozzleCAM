# Go to your project folder (quotes because of spaces)
cd "C:\Users\agger\Documents\PlatformIO\Projects\TTGO T-Journal ESP32"

# If Git isn’t set up yet (do once per machine)
git config --global user.name "Arjen Gerritsen"
git config --global user.email "ag.gerritsen@emergoit.nl"

# Init a repo and create 'main' branch
git init
git branch -M main
